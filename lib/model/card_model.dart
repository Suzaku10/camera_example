class Menu {
  final String cardName;
  final int cardNumber;
  final String name;
  final String imgSourceByString;
  final String expires;
  final int set;

  Menu(
      {required this.cardName,
        required this.cardNumber,
        required this.name,
        required this.imgSourceByString,
        required this.expires,
        required this.set});
}
